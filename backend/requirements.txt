fastapi>=0.110
uvicorn[standard]>=0.29
pandas>=2.0

# 美股数据首选（覆盖全、字段稳定）
yfinance>=0.2.40

# 中文社区常用；A 股最好，美股走东财接口，作为备用源
akshare>=1.13.0

# 新闻 Agent 的生成式问答（可选）。没配 ANTHROPIC_API_KEY 时
# 自动退回检索式，不装这个包也不影响其他功能。
anthropic>=0.116.0
python-dotenv>=1.0.0

# 以下两个只在生成静态资源时用到，不影响后端运行时：
#   pillow     → backend/build_og_image.py     生成社交预览图
#   playwright → backend/build_screenshots.py  生成 README 截图
#                装完还需要 python -m playwright install chromium
pillow>=10.0.0
playwright>=1.40.0
