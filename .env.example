# 复制这个文件为 .env，把 Key 填进去。.env 已在 .gitignore 里，不会被提交。
#
#   copy .env.example .env
#
# Key 在 https://platform.claude.com/ 的 API Keys 页面创建。
# 只有新闻 Agent 的生成式问答用得到；不配也能跑，会自动退回检索式。

# 把等号后面整段替换成你的 Key（真实的 Key 以 sk-ant- 开头）
ANTHROPIC_API_KEY=PASTE_YOUR_KEY_HERE
