# 密钥 —— 绝不能进仓库。API Key 一旦推到 GitHub，
# 几分钟内就会被爬虫扫到并盗用，必须立刻作废重建。
.env
.env.local
*.key
secrets.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/

# 数据缓存
backend/cache/
*.csv
*.parquet
*.db

# 注意：assets/data/symbols.js 和 backend/data/symbols.json 虽然是
# build_symbols.py 生成的，但要入库——否则克隆下来搜索框是空的。
# 刷新：python backend/build_symbols.py

# 编辑器 / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# 日志
*.log
